namespace IncrementalGeneratorSamples;

internal class Program
{
    private static void Main(string[] args)
    {
        // Ignore this error until integration tests get fixed
        Cli.Invoke(args);
    }
}